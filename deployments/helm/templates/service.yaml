apiVersion: v1
kind: Service
metadata:
  name: {{ include "baekjoon-slack-bot.fullname" . }}
  labels:
    {{- include "baekjoon-slack-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "baekjoon-slack-bot.selectorLabels" . | nindent 4 }}
